[
    {
        "enonce": "%version 1 - Court\n\t\n\n\\def\\na{10.0}\n    \\def\\augmentation{-40}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{10.0}    \\def\\augmentation{-40}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 1 - solutions\n\t\n\n\\def\\na{10.0}\n    \\def\\augmentation{-40}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 2 - Court\n\t\n\n\\def\\na{14.0}\n    \\def\\augmentation{25}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{14.0}    \\def\\augmentation{25}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 2 - solutions\n\t\n\n\\def\\na{14.0}\n    \\def\\augmentation{25}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 3 - Court\n\t\n\n\\def\\na{78.6}\n    \\def\\augmentation{-7}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{78.6}    \\def\\augmentation{-7}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 3 - solutions\n\t\n\n\\def\\na{78.6}\n    \\def\\augmentation{-7}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 4 - Court\n\t\n\n\\def\\na{69.4}\n    \\def\\augmentation{-50}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{69.4}    \\def\\augmentation{-50}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 4 - solutions\n\t\n\n\\def\\na{69.4}\n    \\def\\augmentation{-50}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 5 - Court\n\t\n\n\\def\\na{13.0}\n    \\def\\augmentation{-39}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{13.0}    \\def\\augmentation{-39}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 5 - solutions\n\t\n\n\\def\\na{13.0}\n    \\def\\augmentation{-39}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 6 - Court\n\t\n\n\\def\\na{21.1}\n    \\def\\augmentation{-45}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{21.1}    \\def\\augmentation{-45}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 6 - solutions\n\t\n\n\\def\\na{21.1}\n    \\def\\augmentation{-45}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 7 - Court\n\t\n\n\\def\\na{18.9}\n    \\def\\augmentation{-1}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{18.9}    \\def\\augmentation{-1}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 7 - solutions\n\t\n\n\\def\\na{18.9}\n    \\def\\augmentation{-1}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 8 - Court\n\t\n\n\\def\\na{54.3}\n    \\def\\augmentation{17}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{54.3}    \\def\\augmentation{17}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 8 - solutions\n\t\n\n\\def\\na{54.3}\n    \\def\\augmentation{17}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 9 - Court\n\t\n\n\\def\\na{12.0}\n    \\def\\augmentation{19}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{12.0}    \\def\\augmentation{19}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 9 - solutions\n\t\n\n\\def\\na{12.0}\n    \\def\\augmentation{19}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 10 - Court\n\t\n\n\\def\\na{58.1}\n    \\def\\augmentation{-11}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{58.1}    \\def\\augmentation{-11}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 10 - solutions\n\t\n\n\\def\\na{58.1}\n    \\def\\augmentation{-11}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 11 - Court\n\t\n\n\\def\\na{58.8}\n    \\def\\augmentation{-15}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{58.8}    \\def\\augmentation{-15}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 11 - solutions\n\t\n\n\\def\\na{58.8}\n    \\def\\augmentation{-15}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 12 - Court\n\t\n\n\\def\\na{48.5}\n    \\def\\augmentation{38}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{48.5}    \\def\\augmentation{38}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 12 - solutions\n\t\n\n\\def\\na{48.5}\n    \\def\\augmentation{38}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 13 - Court\n\t\n\n\\def\\na{82.9}\n    \\def\\augmentation{45}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{82.9}    \\def\\augmentation{45}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 13 - solutions\n\t\n\n\\def\\na{82.9}\n    \\def\\augmentation{45}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 14 - Court\n\t\n\n\\def\\na{26.6}\n    \\def\\augmentation{41}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{26.6}    \\def\\augmentation{41}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 14 - solutions\n\t\n\n\\def\\na{26.6}\n    \\def\\augmentation{41}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 15 - Court\n\t\n\n\\def\\na{10.0}\n    \\def\\augmentation{-35}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{10.0}    \\def\\augmentation{-35}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 15 - solutions\n\t\n\n\\def\\na{10.0}\n    \\def\\augmentation{-35}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 16 - Court\n\t\n\n\\def\\na{11.0}\n    \\def\\augmentation{-46}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{11.0}    \\def\\augmentation{-46}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 16 - solutions\n\t\n\n\\def\\na{11.0}\n    \\def\\augmentation{-46}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 17 - Court\n\t\n\n\\def\\na{73.6}\n    \\def\\augmentation{-40}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{73.6}    \\def\\augmentation{-40}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 17 - solutions\n\t\n\n\\def\\na{73.6}\n    \\def\\augmentation{-40}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 18 - Court\n\t\n\n\\def\\na{58.5}\n    \\def\\augmentation{8}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{58.5}    \\def\\augmentation{8}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 18 - solutions\n\t\n\n\\def\\na{58.5}\n    \\def\\augmentation{8}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 19 - Court\n\t\n\n\\def\\na{82.9}\n    \\def\\augmentation{22}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{82.9}    \\def\\augmentation{22}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 19 - solutions\n\t\n\n\\def\\na{82.9}\n    \\def\\augmentation{22}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 20 - Court\n\t\n\n\\def\\na{13.0}\n    \\def\\augmentation{32}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{13.0}    \\def\\augmentation{32}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 20 - solutions\n\t\n\n\\def\\na{13.0}\n    \\def\\augmentation{32}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 21 - Court\n\t\n\n\\def\\na{52.7}\n    \\def\\augmentation{47}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{52.7}    \\def\\augmentation{47}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 21 - solutions\n\t\n\n\\def\\na{52.7}\n    \\def\\augmentation{47}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 22 - Court\n\t\n\n\\def\\na{58.9}\n    \\def\\augmentation{-6}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{58.9}    \\def\\augmentation{-6}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 22 - solutions\n\t\n\n\\def\\na{58.9}\n    \\def\\augmentation{-6}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 23 - Court\n\t\n\n\\def\\na{11.0}\n    \\def\\augmentation{5}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{11.0}    \\def\\augmentation{5}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 23 - solutions\n\t\n\n\\def\\na{11.0}\n    \\def\\augmentation{5}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 24 - Court\n\t\n\n\\def\\na{15.4}\n    \\def\\augmentation{-14}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{15.4}    \\def\\augmentation{-14}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 24 - solutions\n\t\n\n\\def\\na{15.4}\n    \\def\\augmentation{-14}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 25 - Court\n\t\n\n\\def\\na{67.7}\n    \\def\\augmentation{-23}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{67.7}    \\def\\augmentation{-23}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 25 - solutions\n\t\n\n\\def\\na{67.7}\n    \\def\\augmentation{-23}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 26 - Court\n\t\n\n\\def\\na{22.7}\n    \\def\\augmentation{13}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{22.7}    \\def\\augmentation{13}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 26 - solutions\n\t\n\n\\def\\na{22.7}\n    \\def\\augmentation{13}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 27 - Court\n\t\n\n\\def\\na{11.0}\n    \\def\\augmentation{-15}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{11.0}    \\def\\augmentation{-15}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 27 - solutions\n\t\n\n\\def\\na{11.0}\n    \\def\\augmentation{-15}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 28 - Court\n\t\n\n\\def\\na{21.5}\n    \\def\\augmentation{-27}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{21.5}    \\def\\augmentation{-27}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 28 - solutions\n\t\n\n\\def\\na{21.5}\n    \\def\\augmentation{-27}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 29 - Court\n\t\n\n\\def\\na{11.4}\n    \\def\\augmentation{9}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{11.4}    \\def\\augmentation{9}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 29 - solutions\n\t\n\n\\def\\na{11.4}\n    \\def\\augmentation{9}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    },
    {
        "enonce": "%version 30 - Court\n\t\n\n\\def\\na{55.1}\n    \\def\\augmentation{-16}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        {\n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100 ), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \\checkaugmentation\n    Déterminer le nouveau prix d'un article coutant $\\num{\\na}~$\\euro{} et dont le nouveau prix a une \\operation\\  de $\\fpeval{abs(\\augmentation)}~\\%$\n\n",
        "reponse": "\\def\\na{55.1}    \\def\\augmentation{-16}\\def\\checkaugmentation{        \\ifthenelse{\\augmentation > 0}        {            \\def\\operation{augmentation}            \\def\\operationtext{augmenté}            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}            \\def\\sign{+}        }        {            \\def\\operation{réduction}            \\def\\operationtext{diminué}            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}            \\def\\sign{-}        }    }    \\checkaugmentation    $\\num{\\rounded}$ \\euro{}",
        "details": "%version 30 - solutions\n\t\n\n\\def\\na{55.1}\n    \\def\\augmentation{-16}\n\n\n\t\n\n\\def\\checkaugmentation{\n        \\ifthenelse{\\augmentation > 0}\n        { \n            \\def\\operation{augmentation}\n            \\def\\operationtext{augmenté}\n            \\def\\calculation{\\fpeval{\\na * (1 + abs(\\augmentation) / 100)}}\n            \\def\\rounded{\\fpeval{round(\\na * (1 + abs(\\augmentation) / 100), 2)}}\n\n            \\def\\sign{+}\n        }\n        { \n            \\def\\operation{réduction}\n            \\def\\operationtext{diminué}\n            \\def\\calculation{\\fpeval{\\na * (1 - abs(\\augmentation) / 100)}}\n\n            \\def\\rounded{\\fpeval{round(\\na * (1 - abs(\\augmentation) / 100), 2)}}\n            \\def\\sign{-}\n        }\n    }\n    \n    \\def\\afficherresultat{\n        \\ifthenelse{\\equal{\\calculation}{\\rounded}}\n          {Nouveau Prix $= \\num{\\rounded}$\\\\ } % Si égaux\n          {Nouveau Prix $= \\num{\\calculation} \\approx \\num{\\rounded}$ \\euro{}\\\\\n          On donne l'arrondi au \\textbf{centième} près car il s'agit d'un prix en euros.} \n      }\n    \\checkaugmentation\n        On utilise la formule suivante : \\\\\n        \n        Nouveau Prix $= \\text{Ancien Prix} \\sign \\text{Ancien Prix} \\times \\left( \\dfrac{t}{100}\\right)  $ \\\\\n        \n        où $t$ est le taux d'augmentation ou de réduction. \n        \n        Ici il s'agit d'une \\operation, avec\n\n $\\text{Ancien Prix} = \\num{\\na}$ et $t = \\num{\\fpeval{abs(\\augmentation)}}$.\n\n\\columnbreak\n        \n        Nouveau Prix $= \\num{\\na} \\sign \\num{\\na} \\times \\dfrac{\\num{\\fpeval{abs(\\augmentation)}}}{100}$\\\\\n        \n        \\phantom{Nouveau Prix} $=\\num{\\na} \\sign \\num{\\fpeval{\\na * abs(\\augmentation) / 100}} $\\\\\n        \n        Ainsi  :\\\\\n        \\afficherresultat\n\n",
        "theme": "\n\nPourcentages\n",
        "prompt": "Depuis la bibliothèque aléatoire"
    }
]