
30
%%
ExoNumber(	number_type='INT',	value_min=2,	value_max=20,	name='numa',	exclude=None)
ExoNumber(	number_type='INT',	value_min=2,	value_max=20,	name='numb',	exclude=None)
ExoNumber(	number_type='INT',	value_min=2,	value_max=20,	name='numc',	exclude=None)
ExoNumber(	number_type='INT',	value_min=2,	value_max=20,	name='numd',	exclude=None)
%%
Fernando a obtenu le nombre $\fpeval{\numa * \numb - (\numc + \numd)}$ à partir des nombres suivants : \numa, \numb, \numc \ et \numd.
\vspace{-0.3cm}\begin{multicols}{2}Voici ses calculs :  \\	
$\numa \times \numb=\fpeval{\numa * \numb}$\\$\numc + \numd=\fpeval{\numc + \numd}$\\	
$\fpeval{\numa * \numb} - \fpeval{\numc + \numd}=\fpeval{\numa * \numb - (\numc + \numd)}$	
	
\columnbreak	
	
Les écrire en une \acc{seule ligne}.	
\end{multicols}

%%
$\numa \times \numb - (\numc + \numd)\\		
= \fpeval{\numa * \numb} - \fpeval{\numc + \numd}=\fpeval{\numa * \numb - (\numc + \numd)}$

%%


%%
$\numa \times \numb - (\numc + \numd)\\		= \fpeval{\numa * \numb} - \fpeval{\numc + \numd}=\fpeval{\numa * \numb - (\numc + \numd)}$
%%
Calcul_ligne
%%


