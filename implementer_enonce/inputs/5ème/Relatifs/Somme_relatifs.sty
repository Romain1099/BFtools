
30
%%
\def\numa{INT -50<=x<50 x!=0 x!=1}
\def\numb{INT -50<=x<0 x!=0 x!=-1}
\def\numc{INT -50<=x<50 x!=0 x!=1}
\def\numd{INT -50<=x<0 x!=0 x!=-1}
%%
Calculer la somme suivante : 	
	
$\affiche{\numa} +\affiche{\numb} + \affiche{\numc} + \affiche{\numd}$ 

%%
$\affiche{\numa} +\affiche{\numb} + \affiche{\numc} + \affiche{\numd} = \fpeval{\numa + \numb + \numc + \numd}$ 

%%


%%
$\fpeval{\numa + \numb + \numc + \numd}$
%%
Relatifs
%%


